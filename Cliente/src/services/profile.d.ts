// src/services/profile.d.ts

// Declaración para la función de obtener el perfil del usuario
export function getProfile(): Promise<{ /* Aquí puedes definir el tipo de respuesta esperado */ }>;

// Declaración para la función de cierre de sesión
export function logout(): Promise<{ /* Aquí puedes definir el tipo de respuesta esperado */ }>;
